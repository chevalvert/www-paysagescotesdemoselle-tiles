[
  {
    "selector": "parent === 'C16'",
    "stroke": {
      "color": "#ff7800",
      "weight": 2,
      "join": "round",
      "cap": "round"
    }
  },

  {
    "selector": "parent === 'C16' && zoom === 3",
    "stroke": {
      "color": "#ff7800",
      "weight": 4,
      "join": "round",
      "cap": "round"
    }
  },

  {
    "selector": "parent === 'C16' && zoom === 4",
    "stroke": {
      "color": "#ff7800",
      "weight": 6,
      "join": "round",
      "cap": "round"
    }
  },

  {
    "selector": "parent === 'C16' && zoom === 5",
    "stroke": {
      "color": "#ff7800",
      "weight": 8,
      "join": "round",
      "cap": "round"
    }
  },

  {
    "selector": "parent === 'C16' && zoom === 6",
    "stroke": {
      "color": "#ff7800",
      "weight": 12,
      "join": "round",
      "cap": "round"
    }
  },

  {
    "selector": "parent === 'C42'",
    "fill": { "pattern": "hatch-16.svg" }
  },

  {
    "selector": "parent === 'C42' && zoom < 5",
    "fill": { "pattern": "hatch-8.svg" }
  },

  { "selector": "parent === 'C50'", "fill": { "color": "#ff7800" }},
  { "selector": "parent === 'C43'", "stroke": { "color": "#ffff00", "weight": 2 }}
]
